<form method="post" style="height: 60px;" action="{{ path('app_trick_delete', {'id': trick.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trick.id) }}">
    <button class="trick_delete_btn"><img width="30" height="30" src="{{asset('img/trash.png')}}"></button>
</form>
