{% extends 'base.html.twig' %}

{% block title %}Edit Trick{% endblock %}

{% block body %}
    <div class="form_supercontainer">
        <h1>Edit Trick</h1>
        <div class="form_container">
            {{ include('trick/_form.html.twig', {'button_label': 'Mettre a jour'}) }}
        </div>
        {{ include('trick/_delete_form.html.twig') }}

        <a class="backto" href="{{ path('app_trick_show', {'slug':trick.slug}) }}">retour au trick</a>

    </div>

{% endblock %}
